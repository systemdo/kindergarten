<h1>EmergencyDatas list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Healthinsurance</th>
            <th>Anothercontact</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('emergencydatas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.healthInsurance }}</td>
            <td>{{ entity.anotherContact }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('emergencydatas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('emergencydatas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('emergencydatas_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
